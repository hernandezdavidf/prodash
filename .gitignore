# Secrets — never commit these
.env
credentials.json
token.json

# David's personal task data — never commit this, whatever folder it lands
# in. This bit us once already: a stray copy in Reports/New folder/ got
# swept into a commit by `git add -A` and had to be un-committed by hand.
dayflow-data.json
**/dayflow-data.json
Reports/

# Disposable / regenerated
.tmp/

# OS / editor cruft
.DS_Store
Thumbs.db
__pycache__/
*.pyc
.venv/
venv/
